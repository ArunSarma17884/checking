# checking

Public firmware binary host for OTA updates of the Energy Meter project (source lives in a
separate private repo). Binaries are also attached to each GitHub Release as downloadable
assets, referenced from an `OTA_UPDATE` MQTT command's `url` field.

This repo intentionally contains no source code — only build outputs.
